server:
  port: 8083
spring:
  application:
    name: billing-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/renthouses_billing}
services:
  notification:
    url: ${NOTIFICATION_SERVICE_URL:http://localhost:8084}
management:
  endpoints:
    web:
      exposure:
        include: health,info
